{
  "name": "scorched-earth-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker + Durable Object that keeps Scorched Earth rooms alive between turns.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "keys": "node scripts/vapid-keys.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
